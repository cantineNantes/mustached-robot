	

	{{ lang('user.fields.email', 'email') | raw }}
	{{ form_input({"name": "email" }, email) | raw }}

	{{ lang('user.fields.firstname', 'firstname') | raw }}
	{{ form_input({"name": "firstname" }, '') | raw }}

	{{ lang('user.fields.lastname', 'lastname') | raw }}
	{{ form_input({"name": "lastname" }, '') | raw }}

	{{ lang('user.fields.twitter', 'twitter') | raw }}
	{{ form_input({"name": "twitter" }, '') | raw }}

	{{ lang('user.fields.companyName', 'companies') | raw }}
	{{ form_input({"name": "company", "id": "companies" }, '') | raw }}

	{{ lang('user.login.reason', 'reason_id') | raw }}
	{{ form_dropdown('reason_id', reasons, reason_id) | raw }}

	{{ form_label(form_checkbox({"name": "public", "checked": public, "value": 1 }, '')~' '~lang('user.login.public')) | raw }}

	{{ form_submit({'name': 'submit', 'value': lang('form.fields.submit'), 'class': 'btn btn-large btn-primary'}) | raw }}

