{% extends "layouts/app.twig" %}

{% block content %}

<h2>{{ user.firstname }} {{ user.lastname }}</h2>

<img src="{{ avatar(user.email, 120) }}" alt=""/>

{% if user.skills %}
<ul>
{% for skill in user.skills %}
	<li>{{ skill.name }}</li>
{% endfor %}
</ul>
{% endif %}

{% endblock %}