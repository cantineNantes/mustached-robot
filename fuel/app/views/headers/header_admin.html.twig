
    <div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container-fluid">
          <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
          <!-- <a class="brand" href="#">Mustached Robot | Admin</a> -->
          <div class="nav-collapse">
            <ul class="nav">
              <li class="dashboard{% if current_url() == "admin" %} active {% endif %}"><a href="{{ url('admin') }}">
                <div class="navIcon"></div>
                <span class="navText">{{ lang('mustached.admin.menu.home') }}</span>
              </a></li>
              <li{% if current_url() == "admin/checkin/stats" %} class="active" {% endif %}><a href="{{ url('admin/checkin/stats') }}" class="statistics">
                <div class="navIcon"></div>
                <span class="navText">{{ lang('mustached.admin.menu.statistics') }}</span>
              </a></li>
              <li{% if current_url() == "admin/user" %} class="active" {% endif %}><a href="{{ url('admin/user') }}" class="coworkers">
                <div class="navIcon"></div>
                <span class="navText">{{ lang('mustached.admin.menu.coworkers') }}</span>
              </a></li>
              <li{% if current_url() == "admin/settings" %} class="active" {% endif %}><a href="#" class="settings">
                <div class="navIcon"></div>
                <span class="navText">{{ lang('mustached.admin.menu.settings') }}</span>
              </a></li>
              <li><a href="#" class="screenMode" id="goScreenMode">
                <div class="navIcon"></div>
                <span class="navText">{{ lang('mustached.admin.menu.screenMode') }}</span>
              </a></li>
            </ul>
          </div><!--/.nav-collapse -->
          <!-- Logged User Box -->
           {% if current_user %}
          <div class="nav-collapse pull-right accountBox">
              <a href="#" data-toggle="dropdown" class="l"><img src="{{ avatar(current_user.email, 40) }}" class="avatar avatar-mini" /></a>
              <ul class="subnav dropdown-menu" >
                <li><a href="{{ url('user/account/edit') }}">{{ lang('mustached.navigation.account.edit') }}</a></li>
                {% if current_user.is_admin %} <li><a href="{{ url('admin') }}">{{ lang('mustached.navigation.account.admin') }}</a></li> {% endif %}
                <li><a href="{{ url('user/auth/logout')}}">Déconnexion</a></li>
              </ul>
          </div>
          {% endif %}
          <!-- /Logged User Box -->
        </div>
      </div>
    </div>
